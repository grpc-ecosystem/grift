grift is not yet published to Maven Central. To be able to run this
example, you first need to install grift to your local Maven repository
(~/.m2).

```sh
$ pushd ../../thrift
$ ./gradlew install
$ popd
```
